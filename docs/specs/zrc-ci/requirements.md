# zrc-ci Requirements

- Build matrix: windows/mac/linux + android + ios
- Reproducible builds where feasible
- Artifact signing:
  - Windows code signing
  - macOS notarization
  - Android signing
  - iOS signing
- Generate and publish:
  - binaries
  - symbols
  - update manifests

