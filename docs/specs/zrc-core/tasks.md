# zrc-core Tasks

- [ ] Introduce persistent store trait + sqlite implementation.
- [ ] Add event hooks for audit log sinks.
- [ ] Add rate limiting hooks (per operator/device).
- [ ] Add transport negotiation priority logic:
  - mesh preferred; fallback ladder based on policy
- [ ] Add consent UX state handling (pending approvals).
- [ ] Add multi-device operator accounts and group pairing (optional).
