# Requirements
- Capture via ScreenCaptureKit (preferred) or CGDisplayStream fallback
- Input via CGEventPost + Accessibility permissions
- Multi-monitor support and coordinate mapping
- Sandboxing considerations (if App Store controller)

