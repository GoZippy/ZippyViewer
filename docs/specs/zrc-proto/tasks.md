# zrc-proto Tasks

- [ ] Freeze v1 tags for currently implemented messages (pairing/session/envelope/control/input).
- [ ] Add DirRecordV1 + DiscoveryTokenV1 (ephemeral discoverability).
- [ ] Add FileTransfer messages (manifest, chunk, resume).
- [ ] Add Clipboard messages (already stubbed).
- [ ] Add Audio messages (optional, later).
- [ ] Add ErrorV1 and standard error code taxonomy.
- [ ] Add compatibility tests: decode/encode golden vectors across platforms.
