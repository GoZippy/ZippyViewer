# zrc-rendezvous Tasks

- [ ] Add auth: bearer token per mailbox or per server.
- [ ] Add per-recipient quotas and eviction policy.
- [ ] Add metrics endpoint (/metrics) optional.
- [ ] Add persistence adapter (Redis) optional.
- [ ] Add admin tooling: list queues, drop queue (self-host only).
